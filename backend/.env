# ============================================
# VitaGloss RD API — Variables de Entorno
# ============================================
# Copia este archivo a .env y rellena los valores

PORT=4000

# MongoDB Local — Conexión a base de datos local
# Para trabajar en local, asegúrate de tener MongoDB instalado y corriendo
MONGODB_URI=mongodb://localhost:27017/vitagloss

# JWT Secret — Cambia esto por una cadena larga y aleatoria
JWT_SECRET=vitagloss_secret_cambia_esto_en_produccion_2026

# Tiempo de expiración del token (7 días)
JWT_EXPIRES_IN=7d

# Frontend URL (para CORS)
FRONTEND_URL=http://localhost:5173

# n8n Webhook — URL que dispara n8n cuando llega un pedido web
# Ejemplo local: http://localhost:5678/webhook/nuevo-pedido
# En producción: https://tu-n8n.tudominio.com/webhook/nuevo-pedido
N8N_WEBHOOK_URL=http://localhost:5678/webhook/nuevo-pedido

# WhatsApp Service local (whatsapp-web.js en puerto 3001)
WA_SERVICE_URL=http://localhost:3001
WA_SECRET=vitagloss_wa_2026

# Número WhatsApp del vendedor principal (formato internacional sin + ni espacios)
VENDOR_PHONE=18492763532
